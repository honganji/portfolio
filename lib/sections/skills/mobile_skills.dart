import 'package:flutter/material.dart';

class MobileSkills extends StatelessWidget {
  const MobileSkills({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("MobileSkills Part");
  }
}
